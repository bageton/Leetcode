class Solution(object):
    def isAnagram(self, s, t):
        res = True
        if len(s) == len(t):
            for i in t:
                if s.count(i) != t.count(i):
                    res = False
            return res
        else:
            return False
